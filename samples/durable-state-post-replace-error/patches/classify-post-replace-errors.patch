--- a/durable_state.py
+++ b/durable_state.py
@@ -16,6 +16,10 @@
 COPY_CHUNK_BYTES = 1024 * 1024


+class ReplacementPublishedError(RuntimeError):
+    """A replacement became visible, but post-replace confirmation failed."""
+
+
 def _replacement_mode(path: Path, default: int) -> int:
     """Return the existing regular file's mode without following symlinks."""
     try:
@@ -54,16 +58,21 @@
                 hook("after-file-fsync")
         os.replace(temporary, path)
         replaced = True
-        if hook:
-            hook("after-replace")
-        flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0)
-        directory_fd = os.open(path.parent, flags)
         try:
-            os.fsync(directory_fd)
-        finally:
-            os.close(directory_fd)
-        if hook:
-            hook("after-directory-fsync")
+            if hook:
+                hook("after-replace")
+            flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0)
+            directory_fd = os.open(path.parent, flags)
+            try:
+                os.fsync(directory_fd)
+            finally:
+                os.close(directory_fd)
+            if hook:
+                hook("after-directory-fsync")
+        except Exception as error:
+            raise ReplacementPublishedError(
+                "replacement is visible, but post-replace confirmation failed"
+            ) from error
     finally:
         if not replaced:
             try:
@@ -110,16 +119,21 @@
                 hook("after-file-fsync")
         os.replace(temporary, path)
         replaced = True
-        if hook:
-            hook("after-replace")
-        flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0)
-        directory_fd = os.open(path.parent, flags)
         try:
-            os.fsync(directory_fd)
-        finally:
-            os.close(directory_fd)
-        if hook:
-            hook("after-directory-fsync")
+            if hook:
+                hook("after-replace")
+            flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0)
+            directory_fd = os.open(path.parent, flags)
+            try:
+                os.fsync(directory_fd)
+            finally:
+                os.close(directory_fd)
+            if hook:
+                hook("after-directory-fsync")
+        except Exception as error:
+            raise ReplacementPublishedError(
+                "replacement is visible, but post-replace confirmation failed"
+            ) from error
     finally:
         if not replaced:
             try:
